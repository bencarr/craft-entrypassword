{% import "_includes/forms.html" as forms %}

{% do view.registerAssetBundle("bencarr\\entrypassword\\assetbundles\\entrypassword\\EntryPasswordAsset") %}

{{ forms.lightswitchField({
    label: 'requiredForAuthenticatedUsers.label' | t('entry-password'),
    instructions: 'requiredForAuthenticatedUsers.instructions' | t('entry-password'),
    id: 'requiredForAuthenticatedUsers',
    name: 'requiredForAuthenticatedUsers',
    on: field['requiredForAuthenticatedUsers'],
}) }}

{{ forms.selectField({
    label: 'cookieExpiration.label'| t('entry-password'),
    instructions: 'cookieExpiration.instructions' | t('entry-password'),
    id: 'cookieExpiration',
    name: 'cookieExpiration',
    type: 'number',
    options: field.getPersistenceOptions(),
    value: field['cookieExpiration'],
}) }}

{{ forms.lightswitchField({
    label: 'displaysInSidebar.label' | t('entry-password'),
    instructions: 'displaysInSidebar.instructions' | t('entry-password'),
    id: 'displaysInSidebar',
    name: 'displaysInSidebar',
    on: field['displaysInSidebar'],
}) }}
