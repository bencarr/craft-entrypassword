{% import "_includes/forms.html" as forms %}

{% do view.registerAssetBundle("bencarr\\entrypassword\\assetbundles\\entrypassword\\EntryPasswordAsset") %}

{{ forms.lightswitchField({
    label: 'Include Authenticated Users',
    instructions: 'Require authenticated users also enter the password to view the entry.',
    id: 'requiredForAuthenticatedUsers',
    name: 'requiredForAuthenticatedUsers',
    value: field['requiredForAuthenticatedUsers']}) }}

{{ forms.textField({
    label: 'Cookie Expiration',
    instructions: 'Number of seconds the authentication cookie should persist after providing a valid password. Enter `0` to persist for the current browser session.',
    id: 'cookieExpiration',
    name: 'cookieExpiration',
    type: 'number',
    min: 0,
    value: field['cookieExpiration'],
}) }}