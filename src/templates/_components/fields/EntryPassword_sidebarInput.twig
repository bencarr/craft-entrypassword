{% import "_includes/forms" as forms %}

{% namespace 'fields' %}
    {{ forms.textField({
        id: name,
        name: name,
        label: field.name,
        fieldId: name ~ '-field-sidebar',
        value: value,
    }) }}
{% endnamespace %}
